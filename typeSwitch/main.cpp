#include<iostream>
#include<memory>
#include<set>
#include<vector>
#include<map>

/*
template <typename L>
struct type_switch
{
  template< typename F >
    void operator()( size_t i, F& f )
    {
      switch ( i ) {
#define CASE_N( N ) \
        case (N): return f.operator()<typename impl::get<L,N>::type>();
        CASE_N(0)
        CASE_N(1)
        CASE_N(2)
        CASE_N(3) // ad nauseam.
      }
    }
};
*/

int main(){
  return 0;
}
