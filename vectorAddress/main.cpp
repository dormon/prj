#include<iostream>
#include<memory>
#include<set>
#include<vector>
#include<map>
#include<cstring>


int main(){
  std::vector<int>a;
  a.push_back(1);
  for(unsigned i=0;i<a.size();++i)std::cout<<&a[i]<<std::endl;std::cout<<std::endl;
  a.push_back(1);
  for(unsigned i=0;i<a.size();++i)std::cout<<&a[i]<<std::endl;std::cout<<std::endl;
  a.push_back(1);
  for(unsigned i=0;i<a.size();++i)std::cout<<&a[i]<<std::endl;std::cout<<std::endl;
  a.push_back(1);
  for(unsigned i=0;i<a.size();++i)std::cout<<&a[i]<<std::endl;std::cout<<std::endl;
  a.push_back(1);
  for(unsigned i=0;i<a.size();++i)std::cout<<&a[i]<<std::endl;std::cout<<std::endl;
  a.push_back(1);
  for(unsigned i=0;i<a.size();++i)std::cout<<&a[i]<<std::endl;std::cout<<std::endl;
  a.push_back(1);
  for(unsigned i=0;i<a.size();++i)std::cout<<&a[i]<<std::endl;std::cout<<std::endl;
  a.push_back(1);
  for(unsigned i=0;i<a.size();++i)std::cout<<&a[i]<<std::endl;std::cout<<std::endl;
  a.push_back(1);
  for(unsigned i=0;i<a.size();++i)std::cout<<&a[i]<<std::endl;std::cout<<std::endl;
  return 0;
}
